apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-app-frontend.fullname" . }}
  labels:
    {{- include "node-app-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-app-frontend.selectorLabels" . | nindent 4 }}
